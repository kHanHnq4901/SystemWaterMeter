// Rank khớp với ORDER_NUM trong DB SYS_MENU
const home = 1,
  analysis = 2,
  device = 3,
  map = 4,
  system = 5,
  logs = 6,
  config = 7;

export { home, analysis, device, map, system, logs, config };
